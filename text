cada numero tiene que tener un value - ok

al tocar un boton, la app tiene que discriminar el numero mediante el value - ok

tiene que permitir que pueda ingresar varios numeros y parsearlos - ok

debe aplicar el operador numerico

el usuario debe poder ingresar el segundo conjunto

finalmente realizar el calculo de primerCOnjunto operadorNumerico segundoConjunto y almacenarlo en el resultado

finalmente debe mostrar el valor de resultado

tiene que tener una funcion de limpiar el resultado cuando el usuario activa de nuevo

variables
    primer conjunto de numeros
    operador numerico
    segundo conjunto
    resultado